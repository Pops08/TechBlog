    {{!-- <article class="post">
      <div class="title">
        <a href="{{post.post_url}}" target="_blank">{{post.title}}</a>
        <span>({{post.post_url}})</span>
      </div>
      <div class="meta">
        {{post.vote_count}} point(s) by {{post.user.username}} on
        {{post.created_at}}
        |
        <!--<a href="/post/{{post.id}}">{{post.comments.length}} comment(s)</a>-->
      </div>
    </article> --}}

    {{!-- <article class="post">
<div class="title">
  {{title}}
</div>
<div class="meta">
  {{vote_count}} {{format_plural "point" vote_count}} by {{user.username}} on {{format_date created_at}}
  |
  {{!-- <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a> --}}
{{!-- </div>
</article> --}} 

<div class="card">
  
  <div class="card-body">
    <a href="/post/{{id}}"><h4 class="card-title">{{title}}</h4></a>
    <h6 class="card-subtitle mb-2 text-muted">Posted By: {{user.username}} on {{format_date created_at}}</h6>
    <p class="card-text">
      {{post_content}}
    </p>
  </div>
</div>